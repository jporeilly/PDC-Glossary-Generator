# Local dev cruft — never copy into the image
.venv/
__pycache__/
*.pyc
*.pyo

# Runtime state (lives in the /data volume, not the image)
people.json
connections.json
settings.json
glossaries.json
tag_dictionary.json
tag_dictionary.json.backup-*
audit_log.json
registries/
.env

# VCS / tooling
.git/
.gitignore
*.log

# Local sample data / uploads (mount these instead if needed)
datasources.sample.csv

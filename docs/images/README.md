# README screenshots

Drop walkthrough screenshots here, then uncomment the matching slot in the
repo [README](../../README.md). Each slot is an HTML comment right under the
step it illustrates, already written with the caption and `width="900"` — so
adding a shot is: save the file, delete the two comment markers.

| File | Illustrates |
| --- | --- |
| `home.png` | Overview — the home page |
| `connect.png` | ① Connect — saved connections, bulk load, the live scan |
| `connect-schema.png` | ① Connect → Schema — the ER diagram with PK/FK edges |
| `connect-files.png` | ① Connect → Files — the MinIO/S3 object browser |
| `review-guide.png` | ② Review — the working-order guide panel |
| `review-grid.png` | ② Review — the grid with inline AI proposal pills |
| `review-duplicates.png` | ② Review — duplicate resolution |
| `govern.png` | ③ Govern — stewardship from the roster |
| `apply-generate.png` | ④ Generate & apply — JSONL + Registry |
| `apply-resolve.png` | ④ Generate & apply — resolve and write back |
| `dictionary-pending.png` | Dictionary — the pending steward review queue |
| `dictionary-governed.png` | Dictionary — the governed vocabulary |
| `settings-snapshot.png` | Settings — state snapshot and restore |
| `ai-agents.png` | The AI agents toolbar and accept pills |
| `pack-export.png` | Export domain pack — the flywheel merge |

Guidelines: capture at a **light theme** and a wide-ish window (the slots
render at 900px), crop to the panel that matters rather than the whole
desktop, and keep the scenario's fictional data on screen — never a real
customer's.

Architecture figures (not screenshots) live in
[`glossary_generator/diagrams/`](../../glossary_generator/diagrams/).

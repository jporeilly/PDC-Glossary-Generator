flask>=3.0
gunicorn>=21.2          # production WSGI server (threaded worker for the streaming endpoints)
psycopg2-binary>=2.9      # PostgreSQL (default driver)
# All four engines ship ready (installed by default since 1.6.20):
pymssql>=2.2             # SQL Server
pymysql>=1.1             # MySQL / MariaDB
oracledb>=2.0            # Oracle (thin mode — no Oracle client needed)
boto3>=1.34             # MinIO / S3 document-store scan (optional)
mammoth>=1.6            # render .docx documents to HTML in the file viewer (optional)
# python-docx>=1.1      # fallback .docx renderer if mammoth isn't installed
